source -- https://github.com/niconielsen32/CameraCalibration

Create a folder named "images" in the same directory
Run getimages.py, press 's' on the keyboard to save images
Run calibration.py that creates pkl files with the camera parameters
Run data.py that reads the pkl files and prints out the camera parameters